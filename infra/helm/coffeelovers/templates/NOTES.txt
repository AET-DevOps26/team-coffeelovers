AI Travel Planner (team-coffeelovers) deployed in namespace {{ .Release.Namespace }}.

Check pod status:
  kubectl get pods -n {{ .Release.Namespace }}

Once the ingress controller is ready, the app is available at:
  https://{{ .Values.ingress.host }}

Frontend:
  https://{{ .Values.ingress.host }}{{ .Values.ingress.paths.frontend }}

Auth endpoints:
  https://{{ .Values.ingress.host }}{{ .Values.ingress.paths.auth }}

Backend API:
  https://{{ .Values.ingress.host }}{{ .Values.ingress.paths.api }}

Check ingress:
  kubectl get ingress -n {{ .Release.Namespace }}

To upgrade after a new image is pushed:
  helm upgrade coffeelovers ./infra/helm/coffeelovers \
    --namespace {{ .Release.Namespace }} \
    --set secrets.postgresPassword=<password>

To uninstall:
  helm uninstall coffeelovers --namespace {{ .Release.Namespace }}
