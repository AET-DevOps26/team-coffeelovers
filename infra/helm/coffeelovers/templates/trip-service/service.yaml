{{- $svc := index .Values.services "trip-service" }}
{{- if $svc.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: trip-service
  labels:
    app.kubernetes.io/name: trip-service
    {{- include "coffeelovers.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ $svc.port }}
      targetPort: {{ $svc.port }}
  selector:
    app.kubernetes.io/name: trip-service
{{- end }}
